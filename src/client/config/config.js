/**
 * @type {{enable: boolean, population: number, collectUrl: string}}
 */
const configHeatmap = {
    enable: true, // enable tracking
    population: 100, // percent
    collectUrl: "http://heatmaphub.com/api/visit"
};

export {configHeatmap};